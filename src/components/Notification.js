import React from 'react'

// shows error messages when creating account or login
const Notification = ({ message }) => {
    return (
            <h4>{message}</h4>
    )
}

export default Notification 